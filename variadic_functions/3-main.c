#include "variadic_functions.h"

/**
 * main - Check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	print_all("ceis", 'H', 0, 3.14, "Holberton");
	print_all("sicf", "String", 100, 'A', 2.718);
	print_all(NULL);
	return (0);
}

